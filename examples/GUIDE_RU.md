# Руководство по тестированию сетевых сценариев

## Как запустить симулятор

```bash
./run.sh
```

После запуска откроется окно Network Simulator. Чтобы открыть проект:
**File > Open Project** — выберите нужный Case.

---

## Общие действия для каждого сценария

1. Откройте проект через **File > Open Project**
2. Нажмите зелёную кнопку **Play** (треугольник) на панели инструментов — это запустит все устройства
3. Дважды кликните на компьютер (PC) — откроется консоль
4. В консоли можно вводить команды (IP уже настроен автоматически)
5. Для проверки связи используйте команду `ping`

### Полезные команды VPCS

| Команда | Что делает |
|---------|-----------|
| `show ip` | Показать текущий IP-адрес |
| `ping 192.168.1.2` | Проверить связь с другим компьютером |
| `ip 10.0.0.1/24` | Задать IP-адрес вручную |

---

## Case 1: Базовый пинг (Basic Ping)

### Что в проекте
- 2 компьютера (PC1, PC2)
- 1 коммутатор (Switch)
- PC1 подключён к Switch, PC2 подключён к Switch

### Что мы проверяем
Могут ли два компьютера в одной сети обмениваться данными через коммутатор.

### Что такое коммутатор
Коммутатор (switch) — это устройство, которое соединяет компьютеры в одну локальную сеть. Он запоминает адреса подключённых устройств и отправляет данные только нужному получателю.

### Настройки сети
- PC1: `192.168.1.1/24`
- PC2: `192.168.1.2/24`
- Оба в одной подсети `192.168.1.0/24`

### Шаги для тестирования

1. Откройте проект **Case1-Basic-Ping**
2. Нажмите **Play** для запуска всех устройств
3. Дважды кликните на **PC1** — откроется консоль
4. Введите команду:
   ```
   ping 192.168.1.2
   ```
5. Вы увидите ответы от PC2:
   ```
   84 bytes from 192.168.1.2 icmp_seq=1 ttl=64 time=0.512 ms
   84 bytes from 192.168.1.2 icmp_seq=2 ttl=64 time=0.408 ms
   ```

### Ожидаемый результат
Пинг проходит успешно. Два компьютера в одной подсети могут общаться через коммутатор.

---

## Case 2: Изоляция подсетей (Subnet Isolation)

### Что в проекте
- 3 компьютера (PC1, PC2, PC3)
- 1 коммутатор (Switch)
- Все три подключены к одному коммутатору

### Что мы проверяем
Что компьютеры в разных подсетях не могут общаться напрямую, даже если подключены к одному коммутатору.

### Что такое подсеть
Подсеть — это группа IP-адресов, которые считаются «соседями». Компьютеры в одной подсети могут общаться напрямую. Для связи между разными подсетями нужен маршрутизатор (router).

### Настройки сети
- PC1: `192.168.1.1/24` (подсеть A)
- PC2: `192.168.1.2/24` (подсеть A)
- PC3: `192.168.2.1/24` (подсеть B — другая!)

### Шаги для тестирования

1. Откройте проект **Case2-Subnet-Isolation**
2. Нажмите **Play**
3. Откройте консоль **PC1**
4. Пингуйте PC2 (та же подсеть):
   ```
   ping 192.168.1.2
   ```
   Результат: **пинг проходит** (одна подсеть)

5. Теперь пингуйте PC3 (другая подсеть):
   ```
   ping 192.168.2.1
   ```
   Результат: **пинг НЕ проходит** (разные подсети)

### Ожидаемый результат
- PC1 → PC2: связь есть (одна подсеть)
- PC1 → PC3: связи нет (разные подсети, нет маршрутизатора)

### Почему так происходит
Коммутатор работает на канальном уровне (Layer 2) — он пересылает кадры по MAC-адресам. Но PC1 видит, что адрес `192.168.2.1` не в его подсети, и пытается отправить пакет через шлюз (gateway). Шлюза нет — пакет никуда не уходит.

---

## Case 3: Изоляция VLAN (VLAN Isolation)

### Что в проекте
- 3 компьютера (PC1, PC2, PC3)
- 1 коммутатор с настроенными VLAN

### Что мы проверяем
Что VLAN разделяет один физический коммутатор на несколько виртуальных сетей.

### Что такое VLAN
VLAN (Virtual LAN) — это технология, которая позволяет разделить один коммутатор на несколько изолированных сетей. Компьютеры в разных VLAN не видят друг друга, хотя подключены к одному коммутатору.

### Настройки сети
- PC1: `10.0.10.1/24` — порт в VLAN 10
- PC2: `10.0.10.2/24` — порт в VLAN 10
- PC3: `10.0.10.3/24` — порт в VLAN 20

Обратите внимание: все IP-адреса в одной подсети! Но VLAN всё равно блокирует связь.

### Шаги для тестирования

1. Откройте проект **Case3-VLAN-Isolation**
2. Нажмите **Play**
3. Откройте консоль **PC1**
4. Пингуйте PC2 (тот же VLAN 10):
   ```
   ping 10.0.10.2
   ```
   Результат: **пинг проходит**

5. Пингуйте PC3 (другой VLAN 20):
   ```
   ping 10.0.10.3
   ```
   Результат: **пинг НЕ проходит**

### Ожидаемый результат
- PC1 → PC2: связь есть (оба в VLAN 10)
- PC1 → PC3: связи нет (PC3 в VLAN 20)

### Почему так происходит
Коммутатор помечает каждый порт номером VLAN. Кадры (frames) передаются только между портами с одинаковым VLAN. Это как если бы PC1/PC2 и PC3 были подключены к разным коммутаторам.

---

## Case 4: Хаб против Коммутатора (Hub vs Switch)

### Что в проекте
- 4 компьютера
- 1 хаб (Hub) — к нему подключены PC1 и PC2
- 1 коммутатор (Switch) — к нему подключены PC3 и PC4
- Хаб и коммутатор соединены между собой

### Что мы проверяем
Разницу в поведении хаба и коммутатора.

### Что такое хаб
Хаб (hub) — простое устройство, которое получает данные от одного компьютера и отправляет их ВСЕМ остальным подключённым устройствам. Это неэффективно и небезопасно.

### Чем коммутатор лучше хаба
Коммутатор запоминает MAC-адреса устройств и отправляет данные только нужному получателю. Это быстрее и безопаснее.

### Настройки сети
- PC1: `10.0.0.1/24` (через хаб)
- PC2: `10.0.0.2/24` (через хаб)
- PC3: `10.0.0.3/24` (через коммутатор)
- PC4: `10.0.0.4/24` (через коммутатор)

### Шаги для тестирования

1. Откройте проект **Case4-Hub-vs-Switch**
2. Нажмите **Play**
3. Откройте консоль **PC1**
4. Пингуйте все компьютеры:
   ```
   ping 10.0.0.2
   ping 10.0.0.3
   ping 10.0.0.4
   ```
   Все пинги должны пройти — все в одной подсети.

5. Для наблюдения за разницей: кликните правой кнопкой на кабель между хабом и PC2, выберите **Start capture**. Потом пингуйте с PC1 на PC3. Вы увидите, что PC2 тоже получает эти пакеты (хаб рассылает всем).

### Ожидаемый результат
- Все компьютеры могут пинговать друг друга
- Хаб рассылает трафик всем портам (broadcast)
- Коммутатор отправляет трафик только получателю (unicast)

---

## Case 5: Звёздная топология (Star Topology)

### Что в проекте
- 6 компьютеров (PC1–PC6)
- 1 центральный коммутатор
- Все компьютеры подключены к одному коммутатору (форма звезды)

### Что мы проверяем
Работу звёздной топологии — самой распространённой схемы подключения в локальных сетях.

### Что такое звёздная топология
Звёздная топология — это когда все устройства подключены к одному центральному узлу (коммутатору). Если один компьютер отключится, остальные продолжат работать.

### Настройки сети
- PC1: `10.0.0.1/24`
- PC2: `10.0.0.2/24`
- PC3: `10.0.0.3/24`
- PC4: `10.0.0.4/24`
- PC5: `10.0.0.5/24`
- PC6: `10.0.0.6/24`

### Шаги для тестирования

1. Откройте проект **Case5-Star-Topology**
2. Нажмите **Play**
3. Откройте консоль **PC1**
4. Проверьте связь со всеми компьютерами:
   ```
   ping 10.0.0.2
   ping 10.0.0.3
   ping 10.0.0.4
   ping 10.0.0.5
   ping 10.0.0.6
   ```
   Все пинги должны пройти.

5. Откройте консоль **PC6** и пингуйте PC1:
   ```
   ping 10.0.0.1
   ```
   Тоже работает — связь двусторонняя.

### Ожидаемый результат
Все 6 компьютеров могут общаться друг с другом через центральный коммутатор. Это демонстрирует, как работает типичная офисная сеть.

---

## Case 6: Несколько коммутаторов (Multi-Switch)

### Что в проекте
- 4 компьютера
- 2 коммутатора, соединённых друг с другом (uplink)
- PC1 и PC2 подключены к Switch1
- PC3 и PC4 подключены к Switch2

### Что мы проверяем
Что компьютеры могут общаться через несколько коммутаторов.

### Что такое uplink
Uplink — это соединение между двумя коммутаторами. Оно позволяет компьютерам из разных коммутаторов общаться, как если бы они были в одной сети.

### Настройки сети
- PC1: `172.16.0.1/24` (Switch1)
- PC2: `172.16.0.2/24` (Switch1)
- PC3: `172.16.0.3/24` (Switch2)
- PC4: `172.16.0.4/24` (Switch2)

### Шаги для тестирования

1. Откройте проект **Case6-Multi-Switch**
2. Нажмите **Play**
3. Откройте консоль **PC1**
4. Пингуйте компьютер на том же коммутаторе:
   ```
   ping 172.16.0.2
   ```
   Результат: **работает** (оба на Switch1)

5. Пингуйте компьютер на другом коммутаторе:
   ```
   ping 172.16.0.4
   ```
   Результат: **тоже работает** (кадры идут через uplink)

6. Откройте консоль **PC4** и пингуйте PC1:
   ```
   ping 172.16.0.1
   ```
   Результат: **работает** в обе стороны

### Ожидаемый результат
Все 4 компьютера могут общаться друг с другом, несмотря на то, что подключены к разным коммутаторам. Uplink-соединение объединяет две группы в одну сеть.

### Почему это важно
В реальных сетях используются десятки коммутаторов. Они соединяются друг с другом uplink-кабелями, создавая единую сеть для сотен компьютеров.

---

## Сводная таблица результатов

| Сценарий | Что тестируем | Результат |
|----------|--------------|-----------|
| Case 1 | Базовый пинг через коммутатор | PC1 ↔ PC2: работает |
| Case 2 | Изоляция подсетей | Одна подсеть: работает, разные: нет |
| Case 3 | Изоляция VLAN | Один VLAN: работает, разные: нет |
| Case 4 | Хаб vs Коммутатор | Всё работает, но хаб рассылает всем |
| Case 5 | Звёздная топология | Все 6 PC общаются через 1 коммутатор |
| Case 6 | Несколько коммутаторов | Все PC общаются через uplink |
| Case 7 | Кольцевая топология | 4 коммутатора в кольце |
| Case 8 | VLAN Trunk | 2 коммутатора с trunk-линком |
| Case 9 | Древовидная топология | Иерархия: ядро > распределение > доступ |
| Case 10 | Broadcast-домен | Цепь хабов — всё видят все |
| Case 11 | Сегментация сети | Отделы изолированы друг от друга |
| Case 12 | Полная mesh-топология | Каждый коммутатор к каждому |

---

## Case 7: Кольцевая топология (Ring Topology)

### Что в проекте
- 4 компьютера (PC1–PC4)
- 4 коммутатора, соединённых в кольцо (Switch1→Switch2→Switch3→Switch4→Switch1)

### Что мы проверяем
Работу кольцевой топологии — устройства соединены в замкнутое кольцо.

### Что такое кольцевая топология
Кольцевая топология — каждый коммутатор соединён с двумя соседями, образуя кольцо. Данные передаются по кольцу от узла к узлу.

### Настройки сети
- PC1: `10.1.0.1/24` → Switch1
- PC2: `10.1.0.2/24` → Switch2
- PC3: `10.1.0.3/24` → Switch3
- PC4: `10.1.0.4/24` → Switch4

### Шаги для тестирования

1. Откройте проект **Case7-Ring-Topology**
2. Нажмите **Play**
3. Откройте консоль **PC1**
4. Пингуйте PC3 (на противоположной стороне кольца):
   ```
   ping 10.1.0.3
   ```
   Результат: **работает** (данные идут по кольцу)

### Ожидаемый результат
Все 4 компьютера могут общаться друг с другом через кольцо коммутаторов.

---

## Case 8: VLAN Trunk

### Что в проекте
- 4 компьютера
- 2 коммутатора, соединённых trunk-линком
- PC1 и PC3 в VLAN 10, PC2 и PC4 в VLAN 20

### Что мы проверяем
Что trunk-порт передаёт трафик нескольких VLAN между коммутаторами.

### Что такое trunk
Trunk — соединение между коммутаторами, которое передаёт кадры нескольких VLAN по одному кабелю. Кадры помечаются тегами VLAN (стандарт 802.1Q).

### Настройки сети
- PC1: `10.10.0.1/24` — VLAN 10, Switch1
- PC2: `10.20.0.1/24` — VLAN 20, Switch1
- PC3: `10.10.0.2/24` — VLAN 10, Switch2
- PC4: `10.20.0.2/24` — VLAN 20, Switch2

### Шаги для тестирования

1. Откройте проект **Case8-VLAN-Trunk**
2. Нажмите **Play**
3. PC1 → PC3 (оба VLAN 10):
   ```
   ping 10.10.0.2
   ```
   Результат: **работает**

4. PC1 → PC2 (разные VLAN):
   ```
   ping 10.20.0.1
   ```
   Результат: **НЕ работает**

5. PC2 → PC4 (оба VLAN 20):
   ```
   ping 10.20.0.2
   ```
   Результат: **работает**

### Ожидаемый результат
- VLAN 10: PC1 ↔ PC3 — связь есть
- VLAN 20: PC2 ↔ PC4 — связь есть
- Между VLAN — связи нет

### Почему это важно
В реальных сетях trunk-линки позволяют расширить VLAN на несколько коммутаторов, сохраняя изоляцию.

---

## Case 9: Древовидная топология (Tree / Hierarchical)

### Что в проекте
- 4 компьютера
- 3 коммутатора: 1 ядро (Core) + 2 распределения (Distribution)
- Трёхуровневая иерархия

### Что мы проверяем
Работу иерархической топологии — типичную структуру корпоративных сетей.

### Что такое древовидная топология
Древовидная (иерархическая) топология — устройства организованы по уровням: ядро (core) → распределение (distribution) → доступ (access/PC). Это стандартная модель для больших сетей.

### Настройки сети
- Core-Switch → Dist-Switch1, Dist-Switch2
- PC1: `192.168.10.1/24`, PC2: `192.168.10.2/24` → Dist-Switch1
- PC3: `192.168.10.3/24`, PC4: `192.168.10.4/24` → Dist-Switch2

### Шаги для тестирования

1. Откройте проект **Case9-Tree-Topology**
2. Нажмите **Play**
3. PC1 → PC2 (один Distribution):
   ```
   ping 192.168.10.2
   ```
   Результат: **работает**

4. PC1 → PC4 (через Core):
   ```
   ping 192.168.10.4
   ```
   Результат: **работает** (кадры идут через ядро)

### Ожидаемый результат
Все компьютеры общаются через иерархию коммутаторов.

---

## Case 10: Broadcast-домен (Hub Chain)

### Что в проекте
- 6 компьютеров
- 2 хаба, соединённых в цепь

### Что мы проверяем
Что цепь хабов создаёт один большой broadcast-домен — все устройства видят весь трафик.

### Что такое broadcast-домен
Broadcast-домен — область сети, в которой широковещательный кадр (broadcast) достигает всех устройств. Хаб пересылает всё всем, поэтому цепь хабов — один broadcast-домен.

### Настройки сети
- PC1–PC6: `10.5.0.1` – `10.5.0.6` (все `/24`)

### Шаги для тестирования

1. Откройте проект **Case10-Broadcast-Domain**
2. Нажмите **Play**
3. Пингуйте любой PC с любого другого:
   ```
   ping 10.5.0.5
   ```
   Результат: **работает**

### Ожидаемый результат
Все 6 компьютеров общаются. Любой трафик виден всем устройствам — это неэффективно и небезопасно для больших сетей.

---

## Case 11: Сегментация сети (Network Segmentation)

### Что в проекте
- 6 компьютеров
- 3 коммутатора (по одному на отдел)
- 3 отдела: Engineering, Sales, HR — каждый в своей подсети

### Что мы проверяем
Что разные отделы изолированы друг от друга через разные подсети.

### Что такое сегментация сети
Сегментация — разделение сети на изолированные сегменты (подсети). Каждый отдел получает свой диапазон IP-адресов. Для связи между отделами нужен маршрутизатор.

### Настройки сети
- Engineering: `10.1.1.1/24`, `10.1.1.2/24` → SW-Engineering
- Sales: `10.2.1.1/24`, `10.2.1.2/24` → SW-Sales
- HR: `10.3.1.1/24`, `10.3.1.2/24` → SW-HR

### Шаги для тестирования

1. Откройте проект **Case11-Network-Segmentation**
2. Нажмите **Play**
3. Eng-PC1 → Eng-PC2 (один отдел):
   ```
   ping 10.1.1.2
   ```
   Результат: **работает**

4. Eng-PC1 → Sales-PC1 (разные отделы):
   ```
   ping 10.2.1.1
   ```
   Результат: **НЕ работает** (разные подсети)

### Ожидаемый результат
- Внутри отдела: связь есть
- Между отделами: связи нет (нет маршрутизатора)

### Почему это важно
Сегментация повышает безопасность: компрометация одного отдела не затрагивает другие.

---

## Case 12: Полная mesh-топология (Full Mesh)

### Что в проекте
- 4 компьютера
- 4 коммутатора, каждый соединён с каждым (6 линков между 4 коммутаторами)

### Что мы проверяем
Работу full mesh-топологии — максимальную избыточность и отказоустойчивость.

### Что такое full mesh
Full mesh — топология, где каждый узел напрямую соединён с каждым другим. Для N узлов нужно N×(N-1)/2 соединений. Это самая надёжная, но и самая дорогая топология.

### Настройки сети
- PC1: `172.20.0.1/24` → Switch1
- PC2: `172.20.0.2/24` → Switch2
- PC3: `172.20.0.3/24` → Switch3
- PC4: `172.20.0.4/24` → Switch4

### Шаги для тестирования

1. Откройте проект **Case12-Full-Mesh**
2. Нажмите **Play**
3. PC1 → PC3:
   ```
   ping 172.20.0.3
   ```
   Результат: **работает**

4. PC2 → PC4:
   ```
   ping 172.20.0.4
   ```
   Результат: **работает**

### Ожидаемый результат
Все компьютеры общаются. Множество путей между узлами обеспечивает избыточность — если один линк откажет, данные пойдут другим путём.

### Почему это важно
Full mesh используется в критически важных сетях (дата-центры, магистральные сети), где простой недопустим.
